int Solution::removeElement(vector<int> &A, int B) {
vector<int>b;
int j=0;
for(int i=0;i<A.size();i++)
{
   if(A[i]==B)
   {
      continue;
    
   }
   else
   {
       A[j]=A[i];
       j++;
   }
}
return(j);
}
